global:
  scrape_interval: 15s

rule_files:
  - alert_rules.yml
    
scrape_configs:
  - job_name: "ops-service"

    metrics_path: /api/v1/metrics  

    static_configs:
      - targets:
          - ops-service:3000
